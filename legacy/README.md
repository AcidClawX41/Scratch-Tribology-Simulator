# legacy/

`salon-scratch-lab.artifact.jsx` es el prototipo original autocontenido
(un único archivo con datos, física y UI mezclados).

**No lo edites.** La fuente de verdad es `packages/core` (motor) y `apps/desktop` (UI).
Se conserva como referencia hasta que exista un generador automático (ver `docs/ROADMAP.md`).
